<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2025 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lineageos.settings"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/device_settings_app_name"
        android:persistent="true"
        android:theme="@style/Theme.SubSettingsBase.Expressive">

        <receiver
            android:name=".BootReceiver"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Tile Activity -->
        <activity
            android:name=".TileActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>

        <!-- Nubia settings root -->
        <activity
            android:name=".NubiaSettingsActivity"
            android:label="@string/nubia_settings_title"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.system" />
            <meta-data
                android:name="com.android.settings.icon_tintable"
                android:value="true" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_triggers"/>
            <meta-data
                android:name="com.android.settings.order"
                android:value="-1000" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/nubia_settings_summary" />
        </activity>

        <!-- Trigger buttons (shown only inside Nubia settings root) -->
        <activity
            android:name=".trigger.TriggerActivity"
            android:label="@string/trigger_buttons_title"
            android:exported="false" />

        <!-- Power cap (shown only inside Nubia settings root) -->
        <activity
            android:name=".power.PowerActivity"
            android:label="@string/powercap_title"
            android:exported="false" />

        <service
            android:name=".trigger.TriggerTileService"
            android:exported="true"
            android:label="@string/trigger_buttons_title"
            android:icon="@drawable/ic_triggers"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <service
            android:name=".trigger.TriggerRecorderService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".power.PowerCapService"
            android:exported="false" />
    </application>
</manifest>
